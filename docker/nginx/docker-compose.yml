version: '3'
services:
  nginx:
        image: nginx:1.17.9-alpine
        container_name: nginx
        volumes:
          - ./conf:/etc/nginx/conf.d
          - ./build:/usr/share/nginx/html
        ports:
          - 80:80
          - 443:443
